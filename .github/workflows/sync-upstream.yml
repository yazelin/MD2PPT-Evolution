name: Sync Upstream

on:
  schedule:
    # 每天 UTC 00:00 (台灣時間 08:00) 執行
    - cron: '0 0 * * *'
  workflow_dispatch: # 允許手動觸發

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.SYNC_TOKEN }}

      - name: Setup Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Add upstream remote
        run: git remote add upstream https://github.com/eric861129/MD2PPT-Evolution.git

      - name: Fetch upstream
        run: git fetch upstream

      - name: Check for updates
        id: check
        run: |
          UPSTREAM_COMMITS=$(git rev-list HEAD..upstream/main --count)
          echo "commits=$UPSTREAM_COMMITS" >> $GITHUB_OUTPUT
          if [ "$UPSTREAM_COMMITS" -gt 0 ]; then
            echo "有 $UPSTREAM_COMMITS 個新的 commit"
          else
            echo "沒有新的更新"
          fi

      - name: Create sync branch and PR
        if: steps.check.outputs.commits > 0
        env:
          GH_TOKEN: ${{ secrets.SYNC_TOKEN }}
        run: |
          BRANCH_NAME="sync-upstream-$(date +%Y%m%d)"
          # 刪除已存在的分支（如果有）
          git push origin --delete $BRANCH_NAME 2>/dev/null || true
          git checkout -b $BRANCH_NAME

          # 嘗試自動合併，處理衝突情況
          if git merge upstream/main --no-edit; then
            PR_BODY="自動同步上游更新。

          ## 更新內容
          包含上游 ${{ steps.check.outputs.commits }} 個新的 commit。

          請檢查後合併。"
          else
            # 有衝突：標記衝突檔案，提交後建立 PR 讓人工處理
            CONFLICTED_FILES=$(git diff --name-only --diff-filter=U)
            git add -A
            git commit --no-edit -m "sync: merge upstream with conflicts

          The following files have conflicts and need manual resolution:
          $CONFLICTED_FILES"
            PR_BODY="⚠️ **自動合併遇到衝突，需要手動解決。**

          ## 衝突檔案
          \`\`\`
          $CONFLICTED_FILES
          \`\`\`

          ## 更新內容
          包含上游 ${{ steps.check.outputs.commits }} 個新的 commit。

          請在本地 checkout 此分支解決衝突後合併。"
          fi

          git push origin $BRANCH_NAME
          sleep 5  # 等待 GitHub 同步
          gh pr create \
            --title "🔄 Sync with upstream $(date +%Y-%m-%d)" \
            --body "$PR_BODY" \
            --base main \
            --head $BRANCH_NAME
